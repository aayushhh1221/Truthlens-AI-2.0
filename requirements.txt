# ─────────────────────────────────────────────
# TruthLens AI 2.0 — Python Dependencies
# ─────────────────────────────────────────────

# Core framework
streamlit==1.38.0
python-dotenv==1.0.1

# Google Gemini (modern unified SDK — replaces deprecated google-generativeai)
google-genai==2.8.0

# Data + visualization
pandas==2.2.3
plotly==5.24.1
numpy==1.26.4

# Image forensics
Pillow==10.4.0
scipy==1.14.1

# Document forensics (OCR + PDF)
pytesseract==0.3.13
pypdf==5.0.1

# Embedding / semantic similarity (Phase 2C)
# NOTE: sentence-transformers + torch are large (~500MB). On memory-constrained
# free-tier deployments (e.g. Streamlit Community Cloud 1GB RAM), you may comment
# these two lines out — TruthLens automatically falls back to a lightweight
# keyword-overlap similarity scorer with zero code changes required.
sentence-transformers==3.2.1
torch==2.4.1
